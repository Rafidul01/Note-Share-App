'use server';

import { ShareNoteInput } from '../types/share.types';

export async function shareNoteAction(input: ShareNoteInput) {
  // TODO: Implement GraphQL mutation
  return { success: true };
}
