'use server';

import { RevokeAccessInput } from '../types/share.types';

export async function revokeAccessAction(input: RevokeAccessInput) {
  // TODO: Implement GraphQL mutation
  return { success: true };
}
