'use server';

import { UpdateNoteInput } from '@/entities/note/model/note.types';

export async function updateNoteAction(input: UpdateNoteInput) {
  // TODO: Implement GraphQL mutation
  return { success: true };
}
